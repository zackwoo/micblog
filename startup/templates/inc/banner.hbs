<header>
    <nav>
        <ul>
            {{#each this}}
                <li><a href="{{href}}" class="{{#if active}}active{{/if}}">{{title}}</a></li>
            {{/each}}            
        </ul>
    </nav>
</header>